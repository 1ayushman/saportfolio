// You can add JavaScript here for more interactive features
// For example, smooth scrolling to sections, animations, etc.

// Example: Smooth scrolling (basic)
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function(e) {
        e.preventDefault();

        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
            target.scrollIntoView({
                behavior: 'smooth'
            });
        }
    });
});